header
    nav(class="navbar navbar-expand-lg navbar-light bg-light shadow-sm")
        a(class="navbar-brand" href="/") GoodRun
        button(class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation")
            span(class="navbar-toggler-icon")
        div(class="collapse navbar-collapse" id="navbarNav")
            ul(class="navbar-nav")
                li(class="nav-item")
                    a(class="nav-link" href="/players") Player View
                li(class="nav-item")
                    a(class="nav-link" href="/organizers") Organizer View
        if user
            p(class="p-2 mb-0 mr-3 text-muted font-weight-light") Welcome, #{user.firstName}!
            a(type="button" class="btn btn-outline-dark" href="/logout") Logout
        else 
            p(class="p-2 mb-0 mr-3 text-muted font-weight-light") Welcome, please login!
            a(type="button" class="btn btn-outline-dark" href="/login") Login

        
